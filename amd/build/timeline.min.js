define(["jquery"],function($){const init=()=>{$('[data-region="timeline"] .videoprogress-timeline__bucket').on("keydown",function(event){if(event.key==="Enter"||event.key===" "){event.preventDefault();$(this).trigger("mouseenter")}})};return{init:init}});